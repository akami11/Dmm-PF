<%= link_to "新規", new_food_path, method: :get%>

<% foods.each do |food| %>
  <table class="table">
    <tr>
      <td><%= link_to food.food_name, food_path(food.id), method: :get %></td>
      <td><%= link_to "削除", food_path(food.id),method: :delete %></td>
    </tr>
  </table>
<% end %>