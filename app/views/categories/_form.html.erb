<%= form_with model: category, local:true do |f| %>
  <%= f.text_field :category_name, class: "" %>
  <p>投稿された内容は沢山の人に見られるものです、不適切な表現には気をつけてください</p>
  <%= f.submit '投稿', class: "btn btn-success" %>
<% end %>

 <% categories.each do |categories| %>
   <table>
     <tr>
       <td><%= categories.category_name %></td>
       <td><%= link_to "編集", edit_category_path(categories.id), method: :get %></td>
       <td><%= link_to "削除", category_path(categories.id),method: :delete %></td>
     </tr>
   </table>
<% end %>